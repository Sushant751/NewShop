{
  "name": "newshop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && node ./node_modules/vite/bin/vite.js build && cd .. && node -e \"const fs=require('fs'); fs.cpSync('frontend/dist', 'dist', {recursive:true}); fs.cpSync('frontend/dist', 'public', {recursive:true});\"",
    "dev": "cd frontend && npm run dev"
  }
}
